package SnakeAndLadder;

public class Jump {
    int startPoint;
    int endpoint;
    public Jump(int startPoint, int endpoint) throws Exception {
        // if(startPoint > endpoint){
        //     throw new Exception("startpoint should be less than endpoint");
        // }
        this.startPoint = startPoint;
        this.endpoint = endpoint;
    }

    
}
